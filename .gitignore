# Dependencies
node_modules

# Build output — never commit these
.next
.open-next
.wrangler

# Lock files — project uses npm (package-lock.json)
yarn.lock
pnpm-lock.yaml
bun.lockb

# OS / editor
.DS_Store
*.local
